// Rendered by Model Tools from the endpoint's LinkML model at the start of every run (SDK-10).
// This placeholder only lets the template compile on its own. Do not edit.

export type Geometry = { type: string; coordinates: unknown };

export type EntityTypeName = string;
